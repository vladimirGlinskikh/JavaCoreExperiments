package stepic.greeting;

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        int number = 0;

        // Поиграйся с примером.
        // Сначала раскомментируй вариант с if-else и запусти, затем
        // закоментируй вариант с if-else и раскоментируй вариант с if
        // запусти, посмотри, сделай выводы.

        //----------------------------------------------------------------
        //вариант с if-else конструкцией

        /*if (number == 0) {
            System.out.println("number = " + ++number);
        } else if (number == 1) {
            System.out.println("number = " + ++number);
        } else if (number == 2) {
            System.out.println("number = " + ++number);
        }*/

        //--------------------------------------------------------------

        // в варианте с if-else конструкцией
        // при условии, что number = 0 на входе
        // на выходе получим number = 0

        //-----------------------------------------------------------------------
        //вариант с if конструкцией

        /*if (number == 0) {
            System.out.println("number = " + ++number);
        }
        if (number == 1) {
            System.out.println("number = " + ++number);
        }
        if (number == 2) {
            System.out.println("number = " + ++number);
        }*/

        //------------------------------------------------------------------------в д

        // в варианте с if конструкцией
        // при условии, что number = 0 на входе
        // на выходе получим
        // number = 1
        // number = 2
        // number = 3
    }
}
